local m = require('util.binding.mod')
local k = require('util.binding.key')
local b = require('util.binding.bind')
local f = require('util.binding.funcs')

return {
    mod = m,
    key = k,
    bind = b,
    funcs = f,
}
