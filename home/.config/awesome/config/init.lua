local path = require('config.path')
local app = require('config.app')
local layout = require('config.layout')

-- Default modkey
local mod = 'Mod4'

return {
    path = path,
    app = app,
    layout = layout,
    mod = mod,
}
