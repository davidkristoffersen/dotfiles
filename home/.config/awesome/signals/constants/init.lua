-- Local
local awesome_signals = require('signals.constants.awesome')
local client_signals = require('signals.constants.client')
local screen_signals = require('signals.constants.screen')


return {
    awesome_signals = awesome_signals,
    client_signals = client_signals,
    screen_signals = screen_signals,
}
