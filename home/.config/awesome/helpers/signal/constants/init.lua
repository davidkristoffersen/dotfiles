-- Local
local awesome_signals = require('helpers.signal.constants.awesome')
local client_signals = require('helpers.signal.constants.client')
local screen_signals = require('helpers.signal.constants.screen')


return {
    awesome_signals = awesome_signals,
    client_signals = client_signals,
    screen_signals = screen_signals,
}
