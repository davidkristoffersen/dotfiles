-- Local
local constants = require('helpers.signal.constants')
local emitters = require('helpers.signal.emitters')
local connecters = require('helpers.signal.connecters')

return {
    constants = constants,
    emitters = emitters,
    connecters = connecters,
}
