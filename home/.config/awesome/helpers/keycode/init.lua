local keys = require("helpers.keycode.keys")
local funcs = require("helpers.keycode.funcs")
local combs = require("helpers.keycode.combinations.init")

return {
    keys = keys,
    funcs = funcs,
    combinations = combs,
}
