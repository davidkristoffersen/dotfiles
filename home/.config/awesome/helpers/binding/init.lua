local m = require('helpers.binding.mod')
local k = require('helpers.binding.key')
local b = require('helpers.binding.bind')
local f = require('helpers.binding.funcs')

return {
    mod = m,
    key = k,
    bind = b,
    funcs = f,
}
